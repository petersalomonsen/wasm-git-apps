# Template — do not commit real tokens.
# Create the secret manually:
#   kubectl create secret generic github-pat \
#     --namespace wasm-git-apps \
#     --from-literal=token=$GITHUB_TOKEN
apiVersion: v1
kind: Secret
metadata:
  name: github-pat
type: Opaque
data:
  token: ""  # base64-encoded GitHub PAT
