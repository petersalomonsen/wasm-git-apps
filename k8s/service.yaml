apiVersion: v1
kind: Service
metadata:
  name: wasm-git-apps
spec:
  selector:
    app: wasm-git-apps
  ports:
    - name: web-app
      port: 8080
      targetPort: 8080
    - name: git-server
      port: 3000
      targetPort: 3000
  type: ClusterIP
